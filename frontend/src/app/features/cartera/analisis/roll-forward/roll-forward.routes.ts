import { Routes } from '@angular/router';
import { RollForwardComponent } from './roll-forward.component';

export const ROLL_FORWARD_ROUTES: Routes = [
  {
    path: '',
    component: RollForwardComponent
  }
];
