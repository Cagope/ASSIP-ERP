import { Routes } from '@angular/router';
import { RevalorizacionListComponent } from './revalorizacion-list.component';

export const revalorizacionRoutes: Routes = [
  { path: '', component: RevalorizacionListComponent }
];
