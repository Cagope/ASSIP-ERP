package co.assip.erp.shared.referencias.catalogos.parentesco;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ParentescoRepository extends JpaRepository<Parentesco, String> {
}
